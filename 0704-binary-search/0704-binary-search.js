/**
 * @param {number[]} nums
 * @param {number} target
 * @return {number}
 */
var search = function(nums, target) {
    let idx = nums.indexOf(target)

    
    return idx 
    
};

// 오름차순 정렬 배열과 타겟
// 배열에서 타켓 찾는 함수 
// 타겟 있으면 타겟의 index. 없으면 -1